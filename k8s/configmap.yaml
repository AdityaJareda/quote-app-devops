apiVersion: v1
kind: ConfigMap
metadata:
  name: quote-app-config
  namespace: quote-app
  labels:
    app: quote-app
data:
  NODE_ENV: "production"
  PORT: "3000"
  APP_NAME: "Quote App"
  APP_VERSION: "1.0.0"

  LOG_LEVEL: "info"
